{% extends 'base.html.twig' %}  

{% block title %}
Lister les d�tails
{% endblock %}

{% block body %}
        {% if is_granted('create', user) %}
        <button id="groupcre" type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#asyncModal" data-href="{{ path('ldapadmin_groupcreate') }}">
            Créer des groupes
        </button>
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#asyncModal" data-href="{{ path('ldapadmin_oucreate') }}">
            Créer des OU
        </button>
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#asyncModal" data-href="{{ path('ldapadmin_userbulk') }}">
            Créer des OU
        </button>
        {% endif %}
    <div class="position-relative">
    <div id="tree" class="col-4 position-absolute">

    </div>

</div>


<div class="modal fade" id="asyncModal" tabindex="-1" aria-labelledby="asyncModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        ...
      </div>
    </div>
  </div>
</div>

{% endblock %}
